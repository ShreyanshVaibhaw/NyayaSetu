# NyayaSetu - Competition Winning Build Prompts
## IndiaAI Innovation Challenge 2026 — Problem Statement 1: AI Enabled Virtual Negotiation Assistance

> **Goal:** Transform NyayaSetu from 5.5/10 to 9/10 competitiveness by implementing all missing and weak features.
> **Priority Order:** Each prompt is numbered by impact. Execute in order.
> **Usage:** Feed each prompt section to Claude Code (or any AI coding assistant) one at a time. Each prompt is self-contained with full context.

---

## PROMPT 1: Wire Voice Input (ASR/TTS) Into Filing UI
**Impact: +2.0 points | Priority: CRITICAL | Effort: ~80 lines in app.py + fix bhashini_client.py**

```
CONTEXT:
I'm building NyayaSetu, an AI-powered MSME dispute resolution app (Streamlit).
The project has backend voice code that is NEVER connected to the UI.

EXISTING FILES (do NOT rewrite, only modify):
- nyayasetu/src/voice/bhashini_client.py — Has speech_to_text(), text_to_speech(), translate() methods
- nyayasetu/src/voice/conversation_engine.py — Has ConversationEngine with multi-step guided intake (udyam -> buyer_name -> buyer_gstin -> invoices -> confirm)
- nyayasetu/app.py — Main Streamlit app, render_file_dispute() is the filing page
- nyayasetu/config.py — Has BHASHINI_API_URL, BHASHINI_INFERENCE_URL, BHASHINI_API_KEY, BHASHINI_USER_ID

TASK:
1. Fix bhashini_client.py to use proper Bhashini ULCA pipeline API format:
   - First call config API (BHASHINI_API_URL) with userId + ulcaApiKey to get serviceId + inferenceApiKey + inference endpoint
   - Then call the inference endpoint with proper pipelineTasks array structure
   - The ASR request format should be:
     {
       "pipelineTasks": [{"taskType": "asr", "config": {"language": {"sourceLanguage": "hi"}, "serviceId": "<from_config_api>"}}],
       "inputData": {"audio": [{"audioContent": "<base64>"}]}
     }
   - Keep the graceful fallback — if API fails, return {"text": "", "source": "fallback"}
   - Add a demo mode fallback: if config.APP_MODE == "DEMO", return simulated transcription text
     like "My Udyam number is UDYAM-RJ-01-0012345 and buyer name is XYZ Engineering"

2. Add voice input to render_file_dispute() in app.py:
   - Add a new section at the TOP of render_file_dispute() with header "🎙️ Voice Filing Assistant"
   - Use st.audio_input("Speak your dispute details") — available in Streamlit 1.33+
   - When audio is recorded:
     a. Encode to base64
     b. Call BhashiniClient().speech_to_text(audio_base64, language=st.session_state["language"])
     c. Display transcription in st.text_area (editable)
     d. Feed transcription to ConversationEngine.process_message()
     e. Show conversation state (what's been extracted: Udyam, buyer name, GSTIN, amounts)
     f. Auto-populate the form fields below from conversation state
   - Add a "🔊 Listen" button next to key system messages that calls text_to_speech() and plays via st.audio()
   - Show a step indicator: "Step 1/5: Tell us your Udyam number" etc.

3. Add voice to render_negotiate() in app.py:
   - Add st.audio_input() as alternative to text input for buyer response
   - Transcribe and fill the text_input field
   - Add TTS playback button for negotiation messages generated by the system

4. Import BhashiniClient and ConversationEngine in app.py (currently not imported)

IMPORTANT RULES:
- Keep all existing text-based form functionality working — voice is an ADDITION, not replacement
- Use st.tabs(["🎙️ Voice Filing", "📝 Manual Filing"]) to separate the two modes
- Handle APP_MODE == "DEMO" gracefully with simulated transcription
- Show a nice info message when Bhashini API is unavailable: "Voice services connecting... Using text mode"
- All voice features must work in Hindi AND English based on selected language
```

---

## PROMPT 2: Add Dispute Sub-Category Classification + Document Checklist
**Impact: +1.5 points | Priority: CRITICAL | Effort: New file + ~60 lines in app.py**

```
CONTEXT:
NyayaSetu has NO dispute sub-category classification and NO document gap detection.
The competition problem statement explicitly requires:
- "classifying dispute sub-categories"
- "identifying missing documentation"

EXISTING FILES:
- nyayasetu/src/vaadpatra/ — Filing module directory
- nyayasetu/src/vaadpatra/document_ocr.py — Has OCR extraction (treats everything as invoice)
- nyayasetu/src/vaadpatra/dispute_builder.py — Builds DisputeCase, sets supporting_documents=[]
- nyayasetu/src/common/models.py — Has DisputeCase pydantic model with supporting_documents: List[str]
- nyayasetu/app.py — render_file_dispute() handles filing UI
- nyayasetu/src/llm/llm_client.py — Has LLMClient with generate() method for Qwen 2.5 via Ollama

TASK:
1. Create nyayasetu/src/vaadpatra/dispute_classifier.py:
   - Class DisputeClassifier with method classify_dispute(dispute_description: str, invoice_data: dict, llm_client) -> dict
   - Define sub-category taxonomy for MSME delayed payment disputes:
     * "delayed_payment_goods" — Payment overdue for goods supplied
     * "delayed_payment_services" — Payment overdue for services rendered
     * "partial_payment" — Buyer paid partially, balance overdue
     * "quality_dispute_payment_withheld" — Buyer withholds citing quality issues
     * "contractual_terms_violation" — Buyer violates agreed credit terms
     * "payment_acknowledgment_missing" — Buyer disputes receiving goods/services
     * "cross_invoice_dispute" — Mismatch between PO and invoice amounts
   - Use LLM (Qwen via Ollama) to classify based on dispute description + invoice details
   - Fallback to rule-based classification if LLM unavailable:
     * If days_overdue > 45 and no partial payment -> delayed_payment_goods
     * If amount_paid > 0 but < total -> partial_payment
     * etc.
   - Return: {"category": "delayed_payment_goods", "confidence": 0.85, "reasoning": "...", "applicable_sections": ["Section 15", "Section 16"]}

2. Create nyayasetu/src/vaadpatra/document_checker.py:
   - Class DocumentChecker with method check_documents(category: str, uploaded_docs: List[str], dispute: DisputeCase) -> dict
   - Define required documents per sub-category:
     {
       "delayed_payment_goods": {
         "mandatory": ["udyam_certificate", "invoice", "purchase_order_or_contract", "delivery_proof"],
         "recommended": ["payment_demand_letter", "correspondence", "bank_statement"],
         "optional": ["quality_inspection_report"]
       },
       "partial_payment": {
         "mandatory": ["udyam_certificate", "invoice", "purchase_order_or_contract", "bank_statement_showing_partial"],
         "recommended": ["payment_demand_letter", "receipt_of_partial_payment"],
         "optional": ["correspondence"]
       },
       // ... for each sub-category
     }
   - Method to auto-detect document type from OCR text (invoice, PO, challan, certificate, letter)
   - Return checklist: {
       "mandatory": [{"doc": "udyam_certificate", "status": "uploaded", "icon": "✅"}, {"doc": "delivery_proof", "status": "missing", "icon": "❌"}],
       "recommended": [...],
       "completeness_score": 0.75,
       "missing_critical": ["delivery_proof"],
       "strength_impact": "Your case strength drops by ~20% without delivery proof"
     }

3. Integrate into app.py render_file_dispute():
   - After the user fills dispute details and before "Assess Case":
     a. Show dispute classification result with category name, confidence badge, applicable sections
     b. Show document checklist as a visual card grid:
        ✅ Udyam Certificate — Uploaded
        ✅ Invoice — Extracted via OCR
        ❌ Delivery Proof — Missing (CRITICAL)
        ⚠️ Payment Demand Letter — Recommended
     c. Show completeness score as a progress bar
     d. Show warning for missing critical documents with specific guidance:
        "Upload delivery challan or signed receipt to strengthen your case by ~20%"

4. Update DisputeBuilder to populate supporting_documents field based on what's uploaded

DESIGN RULES:
- Use st.columns(4) grid for document checklist cards
- Color code: green for uploaded, red for missing mandatory, yellow for recommended
- Make it visually striking — this is a key demo differentiator
- Support both DEMO mode (mock classification) and FULL mode (LLM classification)
```

---

## PROMPT 3: Deepen Multilingual Support (All 8 Languages)
**Impact: +1.0 points | Priority: HIGH | Effort: i18n.py expansion + Bhashini translate integration**

```
CONTEXT:
NyayaSetu declares 8 languages but only English and Hindi have full translations.
Tamil, Marathi, Bengali, Telugu, Kannada, Gujarati have only 4 keys each out of ~100+.
The competition requires "multilingual" support — judges may test in regional languages.

EXISTING FILES:
- nyayasetu/src/common/i18n.py — Translation dictionary, ~242 lines
- nyayasetu/src/voice/bhashini_client.py — Has translate() method (unused)
- nyayasetu/config.py — SUPPORTED_LANGUAGES with 8 languages

TASK:
1. Complete ALL translation keys for ALL 8 languages in i18n.py.
   Current keys that need translation (~100 keys total):
   - Navigation: home, file_dispute, negotiate, dashboard, interest_calc, legal_guide, workspace, about
   - Home: project_title, project_subtitle, hero_title, hero_desc, quick_stats, total_disputes,
     amount_disputed, recovery_rate, avg_days, quick_interest_title, principal_amount, days_overdue,
     estimated_interest, total
   - Module cards: vaadpatra_subtitle, vaadpatra_tag, nyayapredictor_subtitle, nyayapredictor_tag,
     samvadai_subtitle, samvadai_tag, vasoolitracker_subtitle, vasoolitracker_tag
   - Filing: udyam_registration, buyer_details, invoice_details, assess_case, case_assessment,
     settlement_probability, predicted_recovery, estimated_timeline, recommended_strategy,
     similar_cases, generate_documents, check_buyer_risk, extract_invoice_ocr
   - Negotiation: start_negotiation, negotiation_round, buyer_response, process_response,
     current_offer, negotiation_status, escalate_msefc, generate_settlement,
     negotiate_empty_title, negotiate_empty_desc, negotiate_empty_step1-3
   - Workspace: workspace_title, active_case, export_case_json, clear_workspace,
     workspace_cleared, workspace_empty_title, workspace_empty_desc, workspace_empty_step1-3
   - Common: disclaimer, loading, error, success, download, submit, cancel, back, next

   Provide ACCURATE translations for:
   - Tamil (ta)
   - Marathi (mr)
   - Bengali (bn)
   - Telugu (te)
   - Kannada (kn)
   - Gujarati (gu)

   IMPORTANT: Use natural, commonly understood translations. For technical/legal terms
   (like "settlement probability", "MSEFC"), keep the English term and add the local script
   equivalent in parentheses. For example in Hindi: "निपटान संभावना (Settlement Probability)"

2. Add a real-time translation widget in the negotiation page:
   - After each negotiation message (both MSE and buyer side), add a small "🌐 Translate" button
   - On click, call BhashiniClient.translate(text, source_lang, target_lang)
   - Display translated text below the original
   - Cache translations to avoid repeated API calls

3. Add language auto-detection:
   - When user types in the buyer response field, detect if it's Hindi/English/other
   - Show a subtle indicator: "Detected: Hindi 🇮🇳"
   - Auto-translate buyer responses to the MSE's selected language

TRANSLATION QUALITY RULES:
- Use formal/respectful register (not casual)
- Legal terms should be transliterated + translated: "मध्यस्थता (Arbitration)"
- Numbers and dates stay in English numerals
- Company names, Udyam numbers, GSTINs stay in English
- Button labels should be short (2-3 words max in any language)
```

---

## PROMPT 4: LLM-Powered Sentiment Analysis + Smart Tactic Detection
**Impact: +0.8 points | Priority: HIGH | Effort: ~50 lines in tactic_detector.py**

```
CONTEXT:
The current TacticDetector uses basic keyword matching: any("more time" in text for ...).
The competition expects "sentiment analysis and negotiation strategy modelling."
We have Qwen 2.5 14B running via Ollama which can do sophisticated NLP.

EXISTING FILE — nyayasetu/src/samvadai/tactic_detector.py:
- Class TacticDetector with analyze_buyer_response(text, round_number) method
- Currently does: keyword matching into 7 categories (stalling, disputing_amount, threatening,
  ghosting, partial_offer, genuine_hardship, cooperative)
- Returns dict with tactic, confidence, intent, red_flags, recommended_response, good_faith

TASK:
1. Upgrade TacticDetector to use LLM analysis:
   - Add llm_client parameter to __init__
   - New method: analyze_with_llm(buyer_text, round_number, dispute_context) that sends this prompt:

   """
   You are an expert negotiation analyst for MSME delayed payment disputes in India.

   Analyze this buyer's response in Round {round_number} of negotiation:
   "{buyer_text}"

   Dispute context: Claim amount ₹{claim}, overdue {days} days, buyer type: {buyer_type}

   Return ONLY valid JSON:
   {
     "tactic": one of ["stalling", "disputing_amount", "threatening", "ghosting", "partial_offer", "genuine_hardship", "cooperative", "deflecting", "blame_shifting"],
     "sentiment_score": float from -1.0 (hostile) to 1.0 (cooperative),
     "emotions": list of detected emotions ["frustration", "anger", "anxiety", "indifference", "willingness", "guilt"],
     "aggression_level": float 0.0 to 1.0,
     "cooperation_level": float 0.0 to 1.0,
     "good_faith": boolean,
     "intent": brief description of buyer's actual intent,
     "red_flags": list of concerning behaviors detected,
     "confidence": float 0.0 to 1.0,
     "recommended_response": specific advice for the MSE on how to respond,
     "escalation_risk": float 0.0 to 1.0 (likelihood this dispute needs MSEFC),
     "tone_description": brief description like "polite but evasive" or "aggressive and dismissive"
   }
   """

   - Parse LLM JSON response, validate all fields, fall back to keyword method if LLM fails
   - Keep the existing keyword-based method as _analyze_keyword_fallback()

2. Add sentiment visualization in app.py render_negotiate():
   - After processing buyer response, show a sentiment dashboard:
     * Sentiment gauge: -1.0 to 1.0 with color gradient (red → yellow → green)
     * Cooperation vs Aggression bar chart
     * Detected emotions as colored tags/chips
     * Escalation risk meter
     * AI recommendation box with strategic advice
   - Use st.columns() for clean layout
   - Use plotly gauge chart for sentiment score

3. Add negotiation analytics panel:
   - Track sentiment over rounds (line chart showing sentiment_score per round)
   - Show tactic pattern: "Buyer has used stalling tactic in 3 of 4 rounds — consider MSEFC escalation"
   - Predictive insight: "Based on sentiment trajectory, settlement is unlikely within 2 rounds"

IMPORTANT:
- Keyword fallback must ALWAYS work even without LLM
- In DEMO mode, generate realistic mock sentiment data (not just 0.5 for everything)
- The sentiment viz should be visually impressive for demo video
```

---

## PROMPT 5: Activate LLM Settlement Drafting + Professional PDF
**Impact: +0.7 points | Priority: HIGH | Effort: ~60 lines in settlement_drafter.py + PDF rewrite**

```
CONTEXT:
Settlement agreement drafting exists but uses 5 hardcoded text terms.
SETTLEMENT_DRAFT_TEMPLATE exists in prompt_templates.py but is DEAD CODE — never called.
The PDF is plain drawString — looks unprofessional.

EXISTING FILES:
- nyayasetu/src/samvadai/settlement_drafter.py — draft_settlement(), draft_demand_notice(), draft_msefc_reference()
- nyayasetu/src/llm/prompt_templates.py — SETTLEMENT_DRAFT_TEMPLATE at line 85 (never imported)
- nyayasetu/src/reporting/settlement_agreement_pdf.py — basic drawString PDF
- nyayasetu/app.py — render_negotiate() shows settlement as JSON only

TASK:
1. Upgrade settlement_drafter.py:
   - Import SETTLEMENT_DRAFT_TEMPLATE from prompt_templates
   - In draft_settlement(), use LLM to generate rich settlement text:
     * Call llm_client.generate(SETTLEMENT_DRAFT_TEMPLATE.format(...)) with all dispute context
     * Parse the structured response into proper SettlementAgreement fields
     * Generate comprehensive terms including:
       - Recitals (WHEREAS clauses)
       - Definitions (Claim Amount, Settlement Amount, Interest Waived)
       - Payment terms with schedule
       - Consequences of default (revival of full claim + MSEFC reference)
       - Confidentiality clause
       - Without prejudice clause (Section 18 MSMED Act)
       - Governing law (MSMED Act 2006, jurisdiction = state MSEFC)
       - Signature blocks for both parties with witness
     * Fall back to enhanced hardcoded terms if LLM unavailable

   - Add method: generate_installment_plan(total_amount, num_installments, start_date)
     * Support: lump_sum, equal_monthly, front_loaded (50% upfront + rest monthly)
     * Return list of {date, amount, description}

   - Add method: draft_settlement_agreement_text() returning formatted plain text
     (for display in UI before PDF generation)

2. Rewrite settlement_agreement_pdf.py using ReportLab Platypus:
   - Use Flowable-based layout (Paragraph, Table, Spacer, HRFlowable)
   - Professional legal document format:
     * Header: "SETTLEMENT AGREEMENT" centered, bold, 16pt
     * Sub-header: Agreement reference number, date
     * Parties section in a bordered table
     * Recitals with WHEREAS... numbering
     * Numbered clauses with sub-clauses
     * Payment schedule in a proper table with borders
     * Signature block with dotted lines and designation
     * Footer: "This is a computer-generated document from NyayaSetu ODR Platform"
     * Page numbers
   - Use proper fonts, spacing, margins (matching legal document standards)

3. Add settlement PDF download in app.py:
   - In render_negotiate(), after settlement is generated:
     a. Show formatted settlement text in an expander (preview)
     b. Add installment plan selector: st.radio(["Lump Sum (15 days)", "2 Equal Installments", "3 Monthly", "Custom"])
     c. For Custom: st.number_input for num_installments + st.date_input for start_date
     d. Add st.download_button("📄 Download Settlement Agreement PDF")
     e. Also add download buttons for demand notice PDF and MSEFC reference PDF
   - Show all 3 documents as tabs: Settlement | Demand Notice | MSEFC Reference

4. Also activate DISPUTE_ANALYSIS_TEMPLATE:
   - In dispute_builder.py or app.py, after building DisputeCase, call LLM with DISPUTE_ANALYSIS_TEMPLATE
   - Show: Case Validity, Legal Strength (1-10), Risk Factors, Recommended Action
   - Display as a "Case Analysis" card with color-coded strength indicator

DESIGN:
- Settlement preview should look like a real legal document even in the Streamlit UI
- Use st.markdown with styled HTML for the preview
- PDF should be print-ready quality
```

---

## PROMPT 6: Activate LLM Negotiation Messages + AI Buyer Simulator
**Impact: +0.6 points | Priority: MEDIUM | Effort: ~80 lines**

```
CONTEXT:
Negotiation messages are currently f-string templates, not LLM-generated.
NEGOTIATION_STRATEGY_TEMPLATE exists in prompt_templates.py but is DEAD CODE.
There's no buyer-side simulation — judges have to manually type buyer responses.

EXISTING FILES:
- nyayasetu/src/samvadai/negotiation_engine.py — generate_round(), has llm_client
- nyayasetu/src/samvadai/communication_gen.py — template-based messages
- nyayasetu/src/llm/prompt_templates.py — NEGOTIATION_STRATEGY_TEMPLATE line 56

TASK:
1. Upgrade communication_gen.py to use LLM:
   - Add generate_smart_message(message_type, dispute_context, round_number, strategy, llm_client):
     * Use LLM to generate contextually appropriate, legally-grounded messages
     * Prompt should include: dispute details, current round, strategy, previous messages, tactic detected
     * Message should be professional, reference specific legal provisions (Section 15/16), and include
       the counter-offer amount naturally in the text
     * Support Hindi + English output based on selected language
   - Fall back to existing template if LLM fails

2. Create nyayasetu/src/samvadai/buyer_simulator.py:
   - Class BuyerSimulator with method simulate_response(dispute, round_number, mse_offer, buyer_profile)
   - Buyer profiles: "cooperative", "stalling", "hardship", "aggressive", "ghosting"
   - Use LLM to generate realistic buyer responses:
     * Profile-appropriate tone and language
     * Counter-offers that make mathematical sense (not random)
     * Sometimes agrees, sometimes stalls, sometimes escalates
     * May use Hindi phrases mixed with English (realistic for Indian business communication)
   - In DEMO mode, use deterministic responses per round for reproducible demo

3. Add "🤖 AI Buyer Simulation" toggle in app.py render_negotiate():
   - st.toggle("Enable AI Buyer Simulation (Demo Mode)")
   - When ON: instead of text_input for buyer response, auto-generate buyer responses
   - Show the simulated response in a chat-like bubble before processing
   - Allow user to accept or edit the simulated response before processing
   - Profile selector: st.selectbox("Buyer Personality", ["Cooperative", "Stalling", "Aggressive", "Hardship"])

4. Make negotiation feel like a real chat:
   - Use st.chat_message("assistant") and st.chat_message("user") for negotiation log
   - MSE messages in blue bubbles (right-aligned)
   - Buyer messages in gray bubbles (left-aligned)
   - System messages (round info, tactic alerts) in yellow
   - Show typing indicator "Buyer is responding..." during simulation
```

---

## PROMPT 7: Upgrade Prediction Model + Explainability
**Impact: +0.5 points | Priority: MEDIUM | Effort: ~60 lines in outcome_predictor.py**

```
CONTEXT:
Current model is RandomForest trained on 800 synthetic records.
Documentation claims CatBoost. No explainability (SHAP/LIME).
Confidence is hardcoded at 0.74.

EXISTING FILE: nyayasetu/src/nyayapredictor/outcome_predictor.py

TASK:
1. Switch to CatBoost (to match documentation claims):
   - pip install catboost
   - Replace RandomForestRegressor with CatBooostRegressor
   - Use CatBoost's native categorical feature handling (no need for get_dummies)
   - Configure: iterations=500, learning_rate=0.05, depth=6, verbose=False
   - Keep the synthetic data generation but expand to 2000 samples with more variety

2. Add proper confidence estimation:
   - Use CatBoost's staged_predict for prediction intervals
   - Or train an ensemble of 5 models and compute prediction std dev
   - Replace hardcoded 0.74 with actual model confidence per prediction

3. Add SHAP explainability:
   - pip install shap
   - After prediction, compute SHAP values for the input case
   - Return top 5 feature contributions: [("days_overdue", +0.15), ("buyer_type_PSU", -0.08), ...]
   - In app.py, show SHAP waterfall chart using plotly:
     * Base value + feature contributions = final prediction
     * Color-coded: green features help, red features hurt
   - Add plain-English explanation derived from SHAP:
     "Your case has a high settlement probability because: (1) the buyer is a PSU (+12%),
      (2) you have a written agreement (+8%). Risk factors: (1) claim amount is large (-5%)"

4. Integrate similar cases into prediction:
   - Pass similar case outcomes into the prediction context
   - Show: "In 5 similar cases, average recovery was 72% with 83 days resolution"
   - If similar cases disagree with ML prediction, flag it:
     "Note: ML model predicts 77% but historical similar cases show 62% — this case may be harder"

5. Add prediction comparison:
   - Show side-by-side: "Your Case" vs "Average MSME Case" vs "Best Similar Case"
   - Use st.columns(3) with metric cards for each
```

---

## PROMPT 8: Professional Document Generation Upgrade
**Impact: +0.4 points | Priority: MEDIUM | Effort: PDF templates**

```
CONTEXT:
Demand notice PDF is decent but other PDFs are basic drawString text.
Need professional, print-ready legal documents for demo.

EXISTING FILES:
- nyayasetu/src/reporting/demand_notice_pdf.py — Best quality, has sections
- nyayasetu/src/reporting/settlement_agreement_pdf.py — Basic text
- nyayasetu/src/reporting/msefc_reference_pdf.py — Basic text
- nyayasetu/src/reporting/case_summary_pdf.py — Basic text
- nyayasetu/src/reporting/interest_calculation_excel.py — Good, has formulas

TASK:
1. Create a shared PDF base template:
   - Function get_pdf_styles() returning consistent ReportLab styles
   - Header with NyayaSetu logo placeholder + "⚖️" + document title
   - Professional fonts: Helvetica for body, Helvetica-Bold for headers
   - Proper margins: 1 inch all sides
   - Page numbers at bottom center
   - Footer: "Generated by NyayaSetu ODR Platform | Not legal advice"
   - Watermark: "DRAFT" or "COMPUTER GENERATED" (light gray, rotated 45°)

2. Upgrade ALL PDFs to use Platypus flowables:
   - demand_notice_pdf.py: Keep existing structure but add:
     * Bordered header box with "LEGAL NOTICE UNDER SECTION 15-16 MSMED ACT, 2006"
     * Proper party table with borders
     * Invoice-wise breakdown in a Table with alternating row colors
     * Interest calculation section with formula shown
     * Bold deadline text in a highlighted box

   - settlement_agreement_pdf.py: Full legal document format (from Prompt 5)

   - msefc_reference_pdf.py:
     * "APPLICATION UNDER SECTION 18, MSMED ACT, 2006"
     * "Before the Micro and Small Enterprise Facilitation Council, [State]"
     * Complainant/Respondent details table
     * Numbered facts section
     * Prayer section with proper legal formatting
     * Verification + affidavit block

   - case_summary_pdf.py:
     * One-page executive summary
     * Header with case ID, date, parties
     * 4-quadrant layout: Case Details | Financial Summary | Prediction | Recommendation
     * Chart: settlement probability gauge (use plotly to image, embed in PDF)
     * Traffic light indicator for case strength

3. Add PDF preview in Streamlit:
   - Use base64 encoding + st.markdown with iframe to preview PDF inline
   - Or use st.download_button with preview expander showing formatted text version
```

---

## PROMPT 9: Enhanced Recovery Dashboard (VasooliTracker/RecoveryTracker)
**Impact: +0.3 points | Priority: LOW-MEDIUM | Effort: ~40 lines in app.py**

```
CONTEXT:
The Recovery Dashboard exists but may need polish for demo.
The dashboard should showcase data intelligence capabilities.

EXISTING CODE:
- nyayasetu/src/vasoolitracker/dispute_analytics.py — Analytics engine
- nyayasetu/src/vasoolitracker/geo_analytics.py — State-level geo analysis with Folium
- nyayasetu/src/vasoolitracker/buyer_blacklist.py — Repeat defaulter identification
- nyayasetu/src/vasoolitracker/msefc_performance.py — MSEFC council performance scoring
- nyayasetu/app.py — render_dashboard()

TASK:
1. Ensure render_dashboard() has these visual sections:
   a. KPI Cards row: Total Disputes | Amount at Stake | Recovery Rate | Avg Resolution Days
   b. Charts section (2 columns):
      - Dispute trend over time (line chart)
      - Recovery rate by buyer type (bar chart)
      - Amount distribution (histogram)
      - Stage-wise pipeline (funnel chart)
   c. State Heatmap: Folium map showing disputes by state (interactive)
   d. MSEFC Performance Table: Ranked by composite score with color coding
   e. Buyer Blacklist: Top repeat defaulters with risk scores
   f. Sector Analysis: Which MSME sectors face most payment delays

2. Add filters at the top:
   - Date range selector
   - State filter (multiselect)
   - Buyer type filter
   - Amount range slider

3. Make it demo-impressive:
   - Use plotly for all charts (interactive, hover tooltips)
   - Color scheme matching the app theme (navy + gold accents)
   - Add KPI delta arrows (↑↓) showing month-over-month change
   - Export dashboard as PDF report button

4. Add a "My Cases" tab vs "All Cases" tab:
   - My Cases: shows only the user's filed disputes
   - All Cases: shows aggregate analytics from the 5000 synthetic records
```

---

## PROMPT 10: Demo Polish + Edge Cases + Error Handling
**Impact: +0.3 points | Priority: LOW | Effort: scattered fixes**

```
CONTEXT:
Final polish pass for demo readiness. Fix edge cases, add loading states,
ensure every page works flawlessly for a 5-minute demo video.

TASK:
1. Add loading states everywhere:
   - st.spinner("Analyzing your case...") around LLM calls
   - st.spinner("Running prediction model...") around ML predictions
   - st.progress() bar for multi-step operations (OCR → Classification → Prediction)
   - st.toast() for success messages

2. Add demo walkthrough mode:
   - A "▶️ Demo Tour" button on home page
   - When clicked, pre-fills session state with a sample dispute case
   - Automatically navigates through: Filing → Assessment → Prediction → Negotiation → Settlement
   - Shows explanatory tooltips at each step
   - This lets judges see the full flow in 2 minutes without manual input

3. Error handling:
   - If Ollama is not running: show clear message "LLM service offline. Using rule-based mode."
   - If PaddleOCR not installed: "OCR engine loading... Using demo data."
   - If database empty: "No historical data. Using synthetic dataset."
   - NEVER show raw Python tracebacks — always catch and show user-friendly messages

4. Add "About" page content:
   - Project name and tagline
   - Architecture diagram (use st.image with a pre-made diagram, or ASCII art)
   - Technology stack with version numbers
   - Module descriptions (VaadPatra, NyayaPredictor, SamvadAI, RecoveryTracker)
   - Legal framework reference (MSMED Act 2006, Sections 15-19)
   - Privacy statement (DPDP Act 2023 compliance note)
   - Team information placeholder

5. Keyboard shortcuts / Quick actions:
   - Ctrl+Enter to submit forms
   - Quick case selector dropdown for jumping between saved cases

6. Mobile responsiveness note:
   - Add st.set_page_config with responsive layout hints
   - Test that key pages work on tablet-sized screens (judges may test on tablets)
```

---

## EXECUTION ORDER SUMMARY

| Order | Prompt | Impact | Time | Cumulative Score |
|-------|--------|--------|------|-----------------|
| 1 | Voice Input (ASR/TTS) | +2.0 | 1-2 days | 7.5/10 |
| 2 | Dispute Classification + Doc Checklist | +1.5 | 1 day | 8.5/10 |
| 3 | Multilingual i18n + Translation | +1.0 | 1 day | 9.0/10 |
| 4 | Sentiment Analysis + Smart Tactics | +0.8 | 0.5 day | 9.3/10 |
| 5 | LLM Settlement Drafting + Pro PDF | +0.7 | 0.5 day | 9.5/10 |
| 6 | LLM Negotiation + Buyer Simulator | +0.6 | 0.5 day | 9.6/10 |
| 7 | CatBoost + SHAP Explainability | +0.5 | 0.5 day | 9.7/10 |
| 8 | Professional PDF Templates | +0.4 | 0.5 day | 9.8/10 |
| 9 | Dashboard Polish | +0.3 | 0.5 day | 9.9/10 |
| 10 | Demo Polish + Error Handling | +0.3 | 0.5 day | 10/10 |

**Total estimated time: 6-7 days of focused development**

---

## CRITICAL REMINDERS FOR ALL PROMPTS

1. **ALWAYS keep DEMO mode working** — Every feature must have a graceful fallback when Ollama/Bhashini/PaddleOCR is unavailable
2. **NEVER break existing features** — Each prompt adds to, not replaces, existing functionality
3. **Test after each prompt** — Upload to GPU, restart Streamlit, verify no crashes
4. **Indian English context** — Use Indian date formats (DD-MM-YYYY), INR formatting (₹), Indian state names
5. **Legal accuracy matters** — Double-check MSMED Act section references, interest calculations, MSEFC procedures
6. **Judges will test edge cases** — Empty inputs, very large amounts, Hindi text in English fields, etc.
7. **Demo video flow** — Plan the demo to showcase: Voice filing → OCR → Classification → Prediction → Negotiation → Settlement → Dashboard
